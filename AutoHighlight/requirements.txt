# Python runtime dependencies for AutoHighlight
# Tested on Windows with Python 3.10–3.12

# Core dependencies
openai>=1.3.3
python-dotenv>=0.21.0
requests>=2.31.0
jsonschema>=4.19.0

# Azure AI services
azure-ai-vision-imageanalysis==1.0.0b3
azure-identity>=1.16.0
azure-core

# Video processing (FFmpeg-based)
ffmpeg-python>=0.2.0
Pillow>=10.0.0

# Scientific computing
numpy>=1.23.5
scipy>=1.10

# Audio processing (optional for advanced features)
soundfile>=0.12.1
numba<0.60

# Reporting (optional)
fpdf2>=2.7.9

# Notes:
# - FFmpeg: Requires system FFmpeg installation. Download from https://ffmpeg.org/download.html
# - Windows users can install via: winget install ffmpeg
# - ffmpeg-python is a Python wrapper that calls the system FFmpeg binary
